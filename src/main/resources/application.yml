spring:
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      acks: all
      properties:
        enable.idempotence: true

novabank:
  kafka:
    topics:
      transfer-requested: bank.transfers.requested